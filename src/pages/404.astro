---
import Layout from '../layouts/Layout.astro';
import '../styles/global.css';
---

<Layout title="404 - Lost in the Wired">
	<div
		class="container glass max-w-[600px] opacity-0 translate-y-5 animate-[fadeIn_0.5s_cubic-bezier(0.2,0.8,0.2,1)_forwards] m-auto flex p-4"
	>
		<div class="content text-center p-16">
			<div class="error-container flex flex-col items-center gap-6">
				<div class="error-image relative w-[250px] h-[250px] mb-4">
					<img
						src="./lain.gif"
						alt="Lain Iwakura"
						class="character w-full h-full object-contain animate-[float_6s_ease-in-out_infinite]"
						width="307"
						height="498"
						loading="lazy"
					/>
				</div>
				<p class="error-message text-[2rem] tracking-[0.5px] font-bold m-0">
					The page you seek is lost within the Wired
				</p>
				<a href="/" class="button glass">Return to Reality</a>
			</div>
		</div>
	</div>
</Layout>

<style>
	main {
		height: 100%;
		padding: 2rem;
		display: flex;
		align-items: center;
		justify-content: center;
		position: relative;
		z-index: 2;
	}
	.error-message {
		background: linear-gradient(to right, #fff, rgba(255, 255, 255, 0.85));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
	}
	@keyframes fadeIn {
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@keyframes float {
		0%,
		100% {
			transform: translateY(0);
		}
		50% {
			transform: translateY(-10px);
		}
	}

	@media (max-width: 480px) {
		main {
			padding: 1rem;
		}

		.container {
			margin: 0rem;
		}

		.content {
			padding: 3.5rem;
		}

		.error-message {
			font-size: 1.25rem;
		}
		.error-image {
			width: 150px;
			height: 150px;
		}
	}
</style>
